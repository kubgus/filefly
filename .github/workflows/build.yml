name: NodeJS runner

on: push
jobs:
  build-node:
    runs-on: ubuntu-latest
    container: node:18
    steps:
      - name: ✅ Check Versions
        run: |
          node --version
          npm --version
      - name: 🛒 Checkout
        uses: actions/checkout@v3
      - run: npm install
      - name: 🔎 Check IP
        run: curl https://api.ipify.org
      - name: 🚀 Deploy
        run: node .
